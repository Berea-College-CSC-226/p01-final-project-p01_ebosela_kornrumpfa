# Boustrophedon Turtles

## Instructions

**Replace each `**Replace This With Your Answer**` with your answer**

## Section 1

1.a. First, discuss with your team and assign yourselves roles. Try to pick the role you’ve had the least experience in.
     Change roles each day!
```
    Complete the table below at the start of each class period:
```
|                 | Monday | Wednesday | Friday |
|-----------------|--------|-----------|--------|
| Driver          | F      | A         | F      |
| Navigator       | A      | F         | A      |
| Quality Control | A      | A         | A      |

## SECTION 2

2.a. Describe your algorithm below in plain English (no code, yet).

```
    **Replace This With Your Answer**
    The following video demonstrates one possible algorithm. 
    Import the turle and then make it draw a square with the pen size of 20 as a frame of 500 pixels.
    Force the turtle to move in a zig-zap pattern using a green path, until it fills the 500 pixel frame. 
    
    It then fills using a green horizontal boustrophedon pattern, and finishes using black text. 
    To play the video, double-click the image below, and then double-click the video.
    
    
```

1.b. Next, identify the parts of the algorithms that would make useful functions. 
   Describe why the function would be beneficial.

```
    We need the main function and it is useful beacause it makes sure that the code is organized. 
    We also need the draw square function to draw the square.
    We also need a movement functiion to move our turtle according the pattern.
```

1.c. What would you name the function(s), and what inputs would those functions need?

```
    main()  | Inputs: exitOnClick(); 
    drawSquare() | Inputs: command to create turtle; CMD to move turtle forward in a looping pattern. 
    zigZagPattern() | Inputs: command to create turtle; CMD to move turtle startion position; CMD to 
    move turtle forward in a zig zap pattern; CMD to stop. 
```

_Return to the Google Doc to continue this assignment._

___

## SECTION 2:

2.a. Explain the difference between defining a function and calling a function.

```
    Defining a function is programming all the instructions for a specific task, i.e. we defined two
    functions for the task of drawing a square and moving a turtle. 

    Calling a function, is activating the defined function. This is done inside of the main function. 
```

2.b. Describe the challenges of this programming assignment. 
     Did you struggle with getting the screen coordinates to work? 
     Did the programming go smoothly after you selected the algorithms? 
     Are functions still an enigma? Explain.

```
    Our primary issue was creating an exception within the loop. We also struggled with keeping the turtle
    that draws the zigZag, from overlapping the square. How do we exactly generate a 500 Pixel square?
```

___